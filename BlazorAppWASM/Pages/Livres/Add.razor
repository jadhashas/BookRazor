@page "/livres/add"
@inherits AddBase

<div class="page-header">
    <h1>Ajouter un livre</h1>
</div>

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-body">
                    @if (_isLoading)
                    {
                        <div class="text-center">
                            <div class="spinner-border text-primary" role="status">
                                <span class="visually-hidden">Chargement...</span>
                            </div>
                        </div>
                    }
                    else
                    {
                        <LivreForm OnLivreSave="SaveLivreAsync" OnLivreCancel="CancelAdd" />
                    }
                    @if (!string.IsNullOrEmpty(_errorMessage))
                    {
                        <div class="alert alert-danger mt-3" role="alert">
                            @_errorMessage
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>

@* @code { *@
@*     private Livre nouveauLivre = new(); *@
@*     private string? errorMessage; *@

@*     [Inject] *@
@*     public ILivreService LivreService { get; set; } *@

@*     [Inject] *@
@*     public NavigationManager NavigationManager { get; set; } *@

@*     private async Task SaveLivreAsync(Livre livre) *@
@*     { *@
@*         try *@
@*         { *@
@*             // Générer une URL d'image aléatoire *@
@*             livre.ImageUrl = $"https://picsum.photos/200/300?random={Random.Shared.Next(1, 1000)}"; *@
            
@*             await LivreService.AddAsync(livre); *@
@*             NavigationManager.NavigateTo("/livres"); *@
@*         } *@
@*         catch (Exception ex) *@
@*         { *@
@*             errorMessage = $"Erreur lors de l'ajout du livre : {ex.Message}"; *@
@*         } *@
@*     } *@

@*     private void Cancel() *@
@*     { *@
@*         NavigationManager.NavigateTo("/livres"); *@
@*     } *@
@* } *@